1. Create a function that takes an input list scores numbers and returns a list of scores that are higher than 70.
2. implement a program that prompts the user for the name of a variable in camel case and outputs the corresponding name in snake case. Assume that the user’s input will indeed be in camel case. Hints: There are methods in each string variable that checks for casing. You can iterate through each character in a string using for each loop. If unsure of what camel and snake_case is search internet
3. First learn the python input function, then implement a program that prompts the user to enter text, append this string to a list , and show the list in terminal. If the user enters "undo" in any casing,
   remove the last element from list. Hint: What list method allows the removal of last element easily
4. Implement a function that uses list slicing to reverse a string.
5. implement a program that prompts the user for a str of text and then outputs that same text but with all vowels (A, E, I, O, and U) omitted, whether inputted in uppercase or lowercase. use python documentations for string and also watch this video <https://www.youtube.com/watch?v=f4_ZPwvKF5g&t=285s>
6. Create a function that determines if a list containing numbers, in which each subsequent number increases by exactly one a streak, it returns true if list is a single number
7. Create a funcion that returns the best streak in sequence such as [1,2,5,3,4] but you can remove any one element in the list to create a valid streak.
