# Basics

- What is python.
  - Python is **interpreted** language
- Variables in python.
  - Snake Case
  - How its declared
  - How variables differ in python.
  - Basic Mathematical Operation.
    - Kinda like c
    - Division results in float, `//` is used for floor
    - Exponents are done be 2 power 3 such as `2**3`
    - Same `+=` operators
    - E can be added for exponent, such as `16E3`
  - Logical operators that results in boolean values
    - `and` `or` `not`
    - Other operators are Same
    - Result is boolean `True` and `False`
  - Binary values
    - `0b0000001`
  - Type conversions

    - ```python
      binary_string = 0b100

      number =  int(var1, base) #number = 4
      ```

  - Coments in python,
  - Printing in python, and f string: `python

    ```python
    name = Abdul-Rehman
    greeting = f"My name is [name]"

    ```

  - None Type Variable
    - Representing value that has not been determined yet, or default values that will be utilized later
  - Dynamically typed language meaning value of variable can be changed later

- Strings in python
  - F string
  - Initialization , single quotes and double quotes
    - :How to use either of them in string
    - `len()` to determine length of string and also used in lists
    - String Functions
- Function Declaration
  - Order of function Declaration

        - ```python
            if "__name__ == __main__"```

    -Lists

    - ```python
        greetings = ["hello", "hi", "hola"]
        hola = greetings[2]
      ```

    - Basic Lists functions such as append(), pop(), pop(index)
    - Iterating Over a list range method, and for each meathod , draw backs and beifits
    - Write a program to find max in a list using function
    - List sliciing
    - List concatenation
    - `in` keyword

- Global and local scope
- How to declare multiple variables in a single string.
- What is console.
  - Shows output of your program
  - Print function
